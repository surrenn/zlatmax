1. npm i

git commands:

Сначала создать репозиторий на гитхаб

1. git init
2. git add -A
3. git commit -a -m"comment"
4. git remote add origin (ссылка на твой репозиторий)
5. git branch -M main
6. git push -u origin main (только первый раз, дальше git push)